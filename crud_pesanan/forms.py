# from email.policy import default
# from django import forms

# class create_pesanan_form(forms.Form):
#     id_pesanan = forms.CharField(label='ID Pesanan', disabled=True)
#     nama_pesanan = forms.CharField(label='Nama Pesanan', max_length=50)
#     jenis_pesanan = forms.CharField(label='Jenis Pesanan', max_length=50)
#     detail_pesanan = forms.CharField(label='Detail Pesanan', widget=forms.Select(choices=[]))

# class update_pesanan_form(forms.Form):
#     id_pesanan = forms.CharField(label='ID Pesanan', disabled=True)
#     nama_pesanan = forms.CharField(label='Nama Pesanan', max_length=50)
#     jenis_pesanan = forms.CharField(label='Jenis Pesanan', max_length=50)
#     status_pesanan = forms.CharField(label='Status Pesanan', max_length=50)
#     detail_pesanan = forms.CharField(label='Detail Pesanan', widget=forms.Select(choices=[]))